services:
  app:
    build:
      dockerfile: ./deploy/Dockerfile
      context: .
    ports:
      - "80:8080"
    volumes:
      - ./.env:/app/.env:ro
      - ./data/logs:/app/data/logs
    restart: on-failure
